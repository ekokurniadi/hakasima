<?php

if (!defined('BASEPATH'))
    exit('No direct script access allowed');

class Ekspedisi extends MY_Controller {

    protected $access = array('Admin', 'Editor','Author');
    
    function __construct()
    {
        parent::__construct();
        $this->load->model('Ekspedisi_model');
        $this->load->library('form_validation');
    }

    public function index()
    {
        $q = urldecode($this->input->get('q', TRUE));
        $start = intval($this->input->get('start'));
        
        if ($q <> '') {
            $config['base_url'] = base_url() . 'ekspedisi/index.html?q=' . urlencode($q);
            $config['first_url'] = base_url() . 'ekspedisi/index.html?q=' . urlencode($q);
        } else {
            $config['base_url'] = base_url() . 'ekspedisi/index.html';
            $config['first_url'] = base_url() . 'ekspedisi/index.html';
        }

        $config['per_page'] = 10;
        $config['page_query_string'] = TRUE;
        $config['total_rows'] = $this->Ekspedisi_model->total_rows($q);
        $ekspedisi = $this->Ekspedisi_model->get_limit_data($config['per_page'], $start, $q);

        $this->load->library('pagination');
        $this->pagination->initialize($config);

        $data = array(
            'ekspedisi_data' => $ekspedisi,
            'q' => $q,
            'pagination' => $this->pagination->create_links(),
            'total_rows' => $config['total_rows'],
            'start' => $start,
        );
        $this->load->view('header');
        $this->load->view('ekspedisi_list', $data);
        $this->load->view('footer');
    }

    public function read($id) 
    {
        $row = $this->Ekspedisi_model->get_by_id($id);
        if ($row) {
            $data = array(
		'id' => $row->id,
		'ekspedisi' => $row->ekspedisi,
	    );
            $this->load->view('header');
            $this->load->view('ekspedisi_read', $data);
            $this->load->view('footer');
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('ekspedisi'));
        }
    }

    public function create() 
    {
        $data = array(
            'button' => 'Create',
            'action' => site_url('ekspedisi/create_action'),
	    'id' => set_value('id'),
	    'ekspedisi' => set_value('ekspedisi'),
	);

        $this->load->view('header');
        $this->load->view('ekspedisi_form', $data);
        $this->load->view('footer');
    }
    
    public function create_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->create();
        } else {
            $data = array(
		'ekspedisi' => $this->input->post('ekspedisi',TRUE),
	    );

            $this->Ekspedisi_model->insert($data);
            $this->session->set_flashdata('message', 'Create Record Success');
            redirect(site_url('ekspedisi'));
        }
    }
    
    public function update($id) 
    {
        $row = $this->Ekspedisi_model->get_by_id($id);

        if ($row) {
            $data = array(
                'button' => 'Update',
                'action' => site_url('ekspedisi/update_action'),
		'id' => set_value('id', $row->id),
		'ekspedisi' => set_value('ekspedisi', $row->ekspedisi),
	    );
            $this->load->view('header');
            $this->load->view('ekspedisi_form', $data);
            $this->load->view('footer');
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('ekspedisi'));
        }
    }
    
    public function update_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->update($this->input->post('id', TRUE));
        } else {
            $data = array(
		'ekspedisi' => $this->input->post('ekspedisi',TRUE),
	    );

            $this->Ekspedisi_model->update($this->input->post('id', TRUE), $data);
            $this->session->set_flashdata('message', 'Update Record Success');
            redirect(site_url('ekspedisi'));
        }
    }
    
    public function delete($id) 
    {
        $row = $this->Ekspedisi_model->get_by_id($id);

        if ($row) {
            $this->Ekspedisi_model->delete($id);
            $this->session->set_flashdata('message', 'Delete Record Success');
            redirect(site_url('ekspedisi'));
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('ekspedisi'));
        }
    }

    public function _rules() 
    {
	$this->form_validation->set_rules('ekspedisi', 'ekspedisi', 'trim|required');

	$this->form_validation->set_rules('id', 'id', 'trim');
	$this->form_validation->set_error_delimiters('<span class="text-danger">', '</span>');
    }

}

/* End of file Ekspedisi.php */
/* Location: ./application/controllers/Ekspedisi.php */
/* Please DO NOT modify this information : */
/* Generated by Harviacode Codeigniter CRUD Generator 2020-05-01 11:51:40 */
/* http://harviacode.com */